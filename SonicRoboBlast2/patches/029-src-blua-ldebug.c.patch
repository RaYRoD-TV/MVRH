diff --git a/src/blua/ldebug.c b/src/blua/ldebug.c
index 542e209a1..83ca46860 100644
--- a/src/blua/ldebug.c
+++ b/src/blua/ldebug.c
@@ -16,6 +16,7 @@
 #include "lua.h"
 
 #include "lapi.h"
+#include "lauxlib.h"
 #include "lcode.h"
 #include "ldebug.h"
 #include "ldo.h"
@@ -247,12 +248,12 @@ LUA_API int lua_getinfo (lua_State *L, const char *what, lua_Debug *ar) {
     f = clvalue(ci->func);
   }
   status = auxgetinfo(L, what, ar, f, ci);
-  if (strchr(what, 'f')) {
+  if (luaL_strchr(what, 'f')) {
     if (f == NULL) setnilvalue(L->top);
     else setclvalue(L, L->top, f);
     incr_top(L);
   }
-  if (strchr(what, 'L'))
+  if (luaL_strchr(what, 'L'))
     collectvalidlines(L, f);
   lua_unlock(L);
   return status;
